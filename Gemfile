# Local preview only. GitHub Pages ignores this file when it builds the site.
# The `github-pages` gem pins Jekyll and all plugins to the exact versions
# GitHub Pages runs in production, so `bundle exec jekyll serve` renders the
# site the same way it appears live (including jekyll-readme-index, which
# serves README.md as the homepage).
source "https://rubygems.org"

gem "github-pages", group: :jekyll_plugins

# Windows/JRuby timezone data — harmless elsewhere.
gem "tzinfo-data", platforms: [:mingw, :mswin, :x64_mingw, :jruby]
